FoamFile
{
    version 2.0;
    format ascii;
    class dictionary;
    object topoSetDict;
}

actions
(
    {
        name injectionCellSet;
        type cellSet;
        action new;
        source nearestToCell;
        points ((0.35 0.15 0.05));
    }
    {
        name injectionCells;
        type cellZoneSet;
        action new;
        source setToCellZone;
        set injectionCellSet;
    }
);
